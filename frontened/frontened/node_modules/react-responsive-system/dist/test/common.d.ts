import { ScreenClassConfiguration } from '../..';
declare const breakpoints: {
    xs: number;
    sm: number;
    md: number;
    lg: number;
};
export declare function makeScreenClassTester(responsiveSystemConfiguration: Omit<ScreenClassConfiguration<typeof breakpoints>, 'breakpoints'>): (screenClass: keyof typeof breakpoints, expected: keyof typeof breakpoints | 'base') => void;
export {};
